# Breed Portal React Prototype

React conversion of the final HTML prototype.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

A static `preview.html` is included so you can quickly preview the latest UI without installing dependencies.
